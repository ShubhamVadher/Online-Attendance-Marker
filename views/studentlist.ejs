<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Student List</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-gradient-to-br from-gray-100 to-gray-200 min-h-screen py-10 px-4">
    <div class="max-w-4xl mx-auto bg-white rounded-2xl shadow-xl p-8">
      <h1 class="text-3xl font-extrabold text-blue-700 mb-6 text-center">
        Students Present for <%= subjectName %>
      </h1>

      <% if (students && students.length > 0) { %>
        <div class="overflow-x-auto rounded-lg border border-gray-200">
          <table class="min-w-full text-left border-collapse">
            <thead class="bg-blue-100">
              <tr>
                <th class="px-6 py-3 text-sm font-semibold text-gray-700 border-b">#</th>
                <th class="px-6 py-3 text-sm font-semibold text-gray-700 border-b">Name</th>
                <th class="px-6 py-3 text-sm font-semibold text-gray-700 border-b">Registration Number</th>
              </tr>
            </thead>
            <tbody class="text-gray-800 text-sm">
              <% students.forEach((student, index) => { %>
                <tr class="<%= index % 2 === 0 ? 'bg-gray-50' : 'bg-white' %> hover:bg-blue-50 transition">
                  <td class="px-6 py-3 border-b"><%= index + 1 %></td>
                  <td class="px-6 py-3 border-b"><%= student.name %></td>
                  <td class="px-6 py-3 border-b"><%= student.regno %></td>
                </tr>
              <% }); %>
            </tbody>
          </table>
        </div>
      <% } else { %>
        <p class="text-center text-gray-600 mt-6 text-lg font-medium">No students were present for this class.</p>
      <% } %>
    </div>
  </body>
</html>
